import 'package:flutter/material.dart';

class CapsulaScreen extends StatelessWidget {
  const CapsulaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
    );
  }
}